import React, { memo } from 'react'
import './CabinetPickCode.styl'

const CabinetPickCode = () => <div className='cabinet-pick-code'>
  cabinet-pick-code
</div>

export default memo(CabinetPickCode)
